# Riel local state — never committed
.riel/

# Elixir / Phoenix (the skeleton is materialized into derived apps)
_build/
deps/
*.tar
erl_crash.dump

# Generated assets
/priv/static/assets/
/priv/static/cache_manifest.json

# Environment
.env
.env.*
# ...except the canonical template of the portable layer: it IS part of the
# standard (SPEC-config.md §Optional commons points at it) and is tracked.
!skeleton/overlay/.env.example

# OS / editor
.DS_Store